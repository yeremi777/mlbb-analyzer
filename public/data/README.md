# Static Data

Initial public data lives here as JSON files.

Planned files:

- `heroes.json`
- `counters.json` as the counter dataset index
- `counters/<target-hero-id>.json` for per-target counter matchup records
- `rules.json`

For now, keep dataset gathering small and documented before adding large data files or crawlers.
